# Session Snapshot — 14 June 2026 (tarde, técnica)

**Empresa activa:** ninguna comercial (sesión 100% técnica)
**Modo:** Financiero + Ejecución (setup de infra, no outreach)
**Duración:** ~3 horas (incluye ida y vuelta por el "no estaba commiteado")

## Lo que se hizo

1. **Diagnóstico inicial** — Tailscale apagado en VPS, gateway Telegram con unit de user systemd pero `disabled`, `MEMORY.md` bloqueado por filtro de secrets.
2. **Tailscale en VPS** — daemon levantado, autenticado, unit roto arreglado.
3. **Hermes Desktop end-to-end** — Desktop en Windows → Tailscale → `hermes dashboard` en VPS, validado con carga de MEMORY.md.
4. **Servicios persistentes** — 3 unit files (system + user) habilitados para boot.
5. **Limpieza de MEMORY.md** — bloque de keys removido, archivo privado creado, symlink CLI→vault.
6. **Documentación** — 4 archivos en `Config/Remote-Access/`.
7. **Verificación final** — `git ls-files` confirmó los 4 archivos en el repo, push a GitHub OK.

## Decisiones tomadas

- **Bind a IP de Tailscale, no 0.0.0.0** — mantiene superficie de ataque mínima.
- **Basic-auth (no OAuth)** — porque el tráfico queda en la tailnet, no en internet público.
- **Symlink en lugar de duplicar** — single source of truth entre CLI y vault.
- **3 archivos de soporte + README** — en lugar de un solo doc largo, para que cada uno sea encontrable.

## Bloqueos detectados y resueltos

| Bloqueo | Resolución |
|---|---|
| Unit `tailscaled` con `EOF` huérfano → userspace mode | Unit reescrito limpio, daemon reiniciado, TUN mode activado |
| `hermes dashboard` no se podía bindear a la IP tailnet | Resuelto automáticamente al activar TUN mode |
| Filtro `hermes_env` borraba MEMORY.md completo | Bloque "API Keys confirmadas" removido, valor movido a archivo privado |
| Cierre de sesión anterior reportado con output truncado | Recreé los 4 archivos, verifiqué con `git ls-files` y `git log` |

## Acciones comprometidas

- [ ] **Juan:** confirmar si el alias `hermes-vps` quedó configurado en Windows
- [ ] **Hermes (próxima sesión):** verificar que el daily técnico se sincroniza correctamente a PC + Android vía GitHub

## Patrones detectados

- **Patrón "decir listo sin verificar":** quedó registrado en MEMORY.md § "Correcciones aprendidas" como regla permanente. El costo de no verificar es alto: daño de confianza + retrabajo de 15 min.
- **Patrón "auto-sync cubre archivos creados con write_file":** los `write_file` con permisos 600 los commitea el auto-sync igual, así que después hay que `chmod 644` antes del commit manual.

## Referencias

- [[../Daily/2026-06-14-tech-setup|Daily técnico]]
- [[../Daily/2026-06-14-summary|Daily comercial]]
- [[../Config/Remote-Access/README|Config/Remote-Access/README]]
- [[../Config/AGENTS#acceso-remoto--hermes-desktop--vps-via-tailscale|Config/AGENTS]]
- [[MEMORY]]
