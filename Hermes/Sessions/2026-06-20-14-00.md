# Sesión — 2026-06-20

## Empresa(s) trabajadas
**Roggero & Roma** — backup completo del sitio

## Estado al cierre
✅ Todo operativo. Backup funcional con offsite a Google Drive.

## Qué se hizo

### Backup — problemas encontrados y resueltos
1. **asyncPool bug**: el script de Cloudinary solo descargaba 96 de 1.339 imágenes. El pool de concurrencia no esperaba todos los resultados. Fix: reescribí asyncPool con un patrón de cola real (while activo < concurrency). Resultado: **1.343 assets** (1.339 img + 4 video), 0 errores.

2. **MongoDB DB equivocada**: se respaldaba `property-pulse` (vacía) en vez de `test` (69 properties reales). Fix: cambié `--db=property-pulse` por `--db=test` en backup.sh.

3. **Docker cleanup**: los `rm: cannot remove` de archivos root post-backup. Fix: agregué `docker system prune -f` en el cleanup del script.

### Scripts actualizados
- `scripts/cloudinary_backup.js` — asyncPool corregido + persistencia de manifest
- `scripts/backup.sh` — DB `test`, Docker cleanup, paso de Drive upload
- `scripts/upload_drive.py` — script nuevo para subir a Google Drive

### Stack de backup finalizado (5 pasos)
1. MongoDB Atlas (DB `test`) → dump gzip
2. Cloudinary → 1.343 assets descargados
3. GitHub mirror → actualizado
4. Compresión + SHA256 → archive local
5. Google Drive → `RoggeroyRoma/Backups/`

### Drive upload — testeado y funcionando
- Credenciales OAuth OK (`~/.hermes/google_token.json`)
- Upload de 1.1 GB exitoso
- Carpeta destino: `RoggeroyRoma/Backups/`

### Cron seteado
- `0 10 * * 6` = **todos los sábados 10 AM** (Argentina)
- Retention: 30 días local
- Offsite: Google Drive (permanece)
- Cron path: `/home/hermes/.hermes/roggero_crons.txt`

### Estado actual del sitio Roggero & Roma
- MongoDB Atlas: DB `test` con 69 properties + reviews + users + searchterms
- Cloudinary: 1.343 assets (1.339 img + 4 video)
- GitHub: mirror del repo `properties`
- 3 backups en `archives/` (~2.6 GB total)
- Drive: 2 backups en `RoggeroyRoma/Backups/`

## Decisiones tomadas
- Retención 30 días (no 90 como propuse inicialmente — Juan prefirió 30)
- Frecuencia: semanal sábado
- Drive como offsite (1.1 GB por backup — manejable)

## Pendiente
- Ninguno para Roggero. Sistema completo y en producción.

## Contacto para这家
Franco Roma — roggeroyroma.com
