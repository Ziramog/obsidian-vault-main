# Session — 2026-05-09 — VPS Debug + Cron Fix

**Hora:** 12:56 — 13:30
**Modo:** Ejecución — Debug operativo

---

## Problema reportado

Juan: "debug del VPS, problemas con los crons y el envío de mensajes a Telegram"

---

## Hallazgos

### 1. Estado de PM2 y Docker
- PM2 vacío (no había procesos activos de Hermes corriendo)
- Docker: 5 contenedores wolfim corriendo normalmente
- Hermes gateway caido → Hermes Agent funcionando

### 2. Cron jobs — 10 jobs con `last_status: error`
Todos con `RuntimeError: Error code: 400` — mismo patrón:

```
Root cause: Bug en run_agent.py — MiniMax SDK devuelve dict-subclass con model_dump() roto.
Cuando el modelo responde con reasoning_details (thinking blocks), el dict-subclass se incluye
en el request a MiniMax. MiniMax no puede serializarlo → HTTP 400.
```

Fix ya estaba en run_agent.py (líneas 9254-9257, 9321-9322, 7433-7434) pero nunca se aplicó
porque hermes-agent no se restartió después del fix (6 de mayo).

### 3. Hermes Auto-Solve job — script path corrupto
- Campo `script` contenía: `/home/hermes/.hermes/scripts/python3 /home/hermes/.hermes/scripts/hermes-auto-solve.py`
- Ruta inexistente → job siempre fallando
- Corregido: `hermes-auto-solve.py` (relativo a ~/.hermes/scripts/)

### 4. Telegram funcionando
- Gateway OK, Telegram polling OK
- Hermes Health Check reporta: ✅ Gateway, ✅ Telegram, ✅ Hermes

### 5. Errors.log — 176 entradas "Error code: 400"
- Todas pre-restart (última: 12:45)
- post-restart (13:15+): cero nuevos errores 400

---

## Acciones tomadas

| Acción | Resultado |
|---|---|
| `hermes update && hermes gateway restart` | ✅ Aplicado — fix de run_agent.py levantado |
| `hermes cron edit 8af6ce471577 --script hermes-auto-solve.py --no-agent` | ✅ Corregido script path |

---

## Estado post-fix

```
Cron jobs activos:
✅ Memory Backup          — OK (13:15 post-restart)
✅ Daily Email Summary    — OK (13:00)
✅ Auto-Solve             — Script path corregido
⚠️ Morning Report         — Error pre-restart, próxima 10/05 11:00
⚠️ Hermes daily update    — Error pre-restart, próxima 10/05 09:00
⚠️ Daily Skills Report    — Error pre-restart, próxima 10/05 03:00
⚠️ Hermes Health Check    — Error pre-restart, próxima 10/05 04:00
```

---

## Lecciones

1. **Fix en código ≠ fix aplicado.** El fix de run_agent.py existía desde el 6/05 pero no estaba en producción porque no hubo restart. Solución: `hermes update && hermes gateway restart` ahora hace ambos.

2. **Auto-restart del daemon:** Si hermes-agent cae, `Hermes Auto-Solve` (cron cada 15min) lo detecta y levanta. Esta vez no cayó, así que el cron no lo recuperó — por eso hizo falta el restart manual.

3. **Script path en jobs.json:** Cuando Juan actualizó algo manualmente o hubo un bug en la serialización, el campo `script` del job de Auto-Solve quedó corrupto con el path completo + nombre de archivo concatenated.

---

## Tracking

- Next run Memory Backup: 13:30 (verificar OK)
- Next run Auto-Solve: 13:30 (verificar script path corregido)
- Morning Report siguiente: 10/05 11:00