# Session Log — 2026-04-28

> Log de la sesión del martes 28 de abril de 2026.

## Incidente VPS — OOM Killer (resuelto)

### Causas identificadas

1. **OOM Kill - Node process (26/04 05:25)**
   - Proceso node consumió 28GB VM / 6GB RSS → mató OOM Killer
   - Afectó: outreach-api o outreach-daemon (leak o peak)

2. **OOM Kill - Python3 (27/04 20:37)**
   - Proceso python3 consumió 8GB / 7.2GB RSS (UID 0/root)
   - Posible: wolfim-cron-alerts o script python

3. **Script de inicio perdido**
   - `/tmp/start_api.sh` borrado en reinicio → 820+ restart loops de outreach-api

4. **Ataques SSH bruteforce**
   - IPs: 2.57.121.112, 186.96.145.241, 157.66.144.16, 193.24.211.95, 176.241.21.126

### Fixes aplicados

- Swap 2GB ya existía (no se tocó)
- Límites PM2: `--max-memory-restart 300M` (outreach-daemon), `200M` (wolfim-alerts, wolfim-cron-alerts)
- Swappiness: `vm.swappiness=10` (kernel usa swap antes de matar procesos)
- Fail2ban instalado y activo
- Script `/root/start_api.sh` recreado con vars de entorno necesarias

### Especificaciones VPS

| Recurso | Valor |
|---|---|
| RAM total | 8GB |
| Swap | 2GB |
| RAM disponible post-fixes | ~7GB |

### Servicios activos

- Docker: wolfim-api, wolfim-cron-alerts, wolfim-alerts, wolfim-crawler, wolfim-agent
- PM2: outreach-daemon, outreach-api, wolfim-alerts, wolfim-cron-alerts
- Nginx: puertos 80, 443, 3443
- Fail2ban: SSH protection activo

### Recomendaciones pendientes

- [ ] Monitor de memoria con alertas (RAM > 80%)
- [ ] oom_score_adj de PM2 para prioridad
- [ ] Límites memory en containers Docker
- [ ] SSH key-only (deshabilitar password auth)
- [ ] Reboot semanal automático

### Comandos útiles

```bash
# Memoria
free -m
ps aux --sort=-%mem | head -15

# OOM logs
grep -i 'oom' /var/log/syslog | tail -20

# PM2
pm2 list
pm2 logs outreach-api --lines 50

# Fail2ban
systemctl status fail2ban
fail2ban-client status sshd
```

---

## Estado Telegram

- Config: `TELEGRAM_HOME_CHANNEL: 1479438002` (Juanchi777, DM)
- Canal existe en channel_directory.json
- **Necesita restablecerse** — sin sesión activa en este momento

---

## Temas abiertos

- Franco Roma: specs pendientes + audio meeting analysis
- Luis Farias: follow-up Friday
- Juan Quital: hermano revisa semana que viene
- Outreach inicial: RIVAS Inmuebles, Conforti Propiedades
